# Scroll-area

`frontend/packages/ui/src/components/scroll-area.tsx`

## `ScrollArea` {#scrollarea}

**Component**

```typescript
function ScrollArea({
  className,
  children,
  ...props
}: ScrollAreaPrimitive.Root.Props)
```

## `ScrollBar` {#scrollbar}

**Component**

```typescript
function ScrollBar({
  className,
  orientation = "vertical",
  ...props
}: ScrollAreaPrimitive.Scrollbar.Props)
```
