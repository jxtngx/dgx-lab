# DGX Lab frontend — copy to .env.local and adjust as needed

# API base URL (default uses Next.js rewrite to localhost:8000)
# Override if the backend is at a different address
# NEXT_PUBLIC_API_URL=/api
